# AccountBook 本地优先迁移记录

## 迁移目标

新版以 `D:\Codex\AccountBook` 为项目根目录，`CodexAccountBook-master` 只作为只读参考。应用从第一阶段使用 Tauri v2、React、TypeScript、Vite、Tailwind CSS、shadcn/ui 和 SQLite。SQLite 是唯一持久化数据库，页面不得直接执行 SQL，统一通过 Repository / Service 层访问。

## 1. 旧版功能清单

- 页面：仪表盘、流水列表、导入账单、手动记账、选项管理。
- 仪表盘：个人收支、结余、交易数、过手资金、公费报销、年度分类表、月度趋势、分类/标签图表、待报销和待转出清单。
- 流水：按月浏览、全库关键词表达式搜索、字段筛选、排序、选择、单笔与批量编辑、复制、删除、跨月草稿及未保存保护。
- 录入：日期时间、账户、收支、金额、分类、标签、状态、备注、对方和支付方式联动。
- 选项：账户、分类和标签的排序、新增、重命名、删除及默认标签映射。
- 导入：支付宝/微信 CSV、XLSX，历史 Excel，表头识别、金额清洗、过滤、去重和预览。
- 导出：旧版 Excel 流水备份；新版改为标准 CSV 和完整 SQLite 备份恢复。
- 本地表格：列宽、列序、选择、键盘操作、复制粘贴、日期显示和特殊状态颜色。

## 2. 旧版数据结构分析

- 数据表：`transactions`、`option_items`、`category_tag_mappings`、兼容表 `income_category_tag_mappings`、`app_migrations`。
- 流水使用 UUID 文本主键、带符号 `REAL` 金额、本地日期时间文本和唯一 `import_hash`。
- 旧库共有 3,483 条流水，范围为 `2023-06-01 00:00:00` 至 `2026-08-08 02:41:12`；金额符号、日期及两位小数均通过检查。
- 旧版以文本关联账户、分类和标签；删除选项只影响后续选择，历史值继续保留。
- 特殊分类通过 `system_key` 维持身份：公费垫付、垫付报销、过手转入、过手转出。
- 历史停用分类包括支出“礼物”2 条、收入“转账红包”1 条。
- 旧版手工录入的去重策略不一致。新版只对外部导入自动去重，手动录入允许相同内容。
- 自动过滤记录继续作为当前应用会话临时数据，不写入 SQLite。

## 3. 新版数据模型

- `schema_migrations`：记录应用级迁移版本、名称、校验值和执行时间。
- `books`：首版使用单一默认账本，预留 UUID、币种和时区。
- `accounts`、`categories`、`tags`：稳定 ID、排序、启用状态；分类保存类型、系统角色和默认标签。
- `transactions`：金额使用带符号整数分；保存类型、时间、账户、可空分类/标签、状态代码、备注、对方、支付方式、来源原分类、来源、导入指纹、创建/更新时间及软删除时间。
- `source_category_mappings`：保存外部来源分类到业务分类的映射。未匹配项保留来源原值并进入待分类状态。
- `app_settings`：保存当前账本、列布局和筛选偏好，使完整备份包含应用状态。
- 所有业务表包含 `book_id`；实体使用 UUID，并保留 `updated_at`、`deleted_at` 以便未来同步。

## 4. 旧功能到新功能的对应关系

| 旧版 | 新版 |
| --- | --- |
| Streamlit 侧边栏 | 桌面侧边导航，窄屏底部导航 |
| 仪表盘 | 响应式概览页，保持旧统计口径 |
| 流水列表 | 桌面数据表格与手机卡片列表 |
| 手动记账 | 全局快速记账弹窗，窄屏全屏表单 |
| 选项管理 | 设置页账户、分类、标签管理 |
| 支付宝/微信导入 | 导入中心与来源分类映射 |
| 历史 Excel | 已从当前产品移除 |
| Excel 备份 | UTF-8 CSV 与完整本地备份恢复 |
| 浏览器组件草稿 | React 内存草稿与统一离开保护 |

## 5. 新项目目录

```text
src/
├─ components/       通用 UI、shadcn/ui、导航与布局
├─ pages/            概览、流水、导入、设置
├─ features/         流水、分类、分析、导入、备份、同步
├─ db/               SQLite 连接和 Repository 实现
├─ services/         业务服务接口与实现
├─ types/            TypeScript 类型
├─ hooks/            React Hooks
└─ utils/            金额、日期、搜索和统计工具

src-tauri/
├─ migrations/       编号 SQL 迁移
└─ src/              Tauri 命令、便携数据库、备份恢复
```

## 6. 分阶段迁移计划

1. 创建 React/Vite/Tauri 工程、SQLite 迁移和应用外壳。
2. （已取消）旧库只读复制转换和迁移报告不再属于当前产品。
3. 实现 Repository/Service、快速录入、流水和选项管理。
4. 实现支付宝/微信和标准 CSV 导入导出；历史 Excel 入口已取消。
5. 实现旧统计口径、特殊业务、图表和跟踪清单。
6. 实现完整备份恢复、校验和自动回滚备份。
7. 完成桌面及窄屏验收；首版不构建 Android/iOS。

## 7. 已确认事项

- 根目录为 `D:\Codex\AccountBook`，不创建嵌套的新版项目目录。
- SQLite 是唯一数据库，不使用 Dexie 或 IndexedDB。
- 保留支付宝/微信 CSV/XLSX 和标准 CSV；历史 Excel 不再支持。
- 旧数据库先备份、再复制转换，绝不原地修改。
- 自动过滤明细仅保留在当前会话。
- 仅外部导入自动去重；手动录入允许真实重复。
- 无法明确映射的来源分类保留原值并进入待分类。
- 首版备份不加密；不实现登录、服务器、云同步和预算。

## 8. 当前产品边界补充（2026-08-10）

- 当前导入中心只保留支付宝 CSV/XLSX、微信 CSV/XLSX 和标准 CSV。
- “旧版 Excel”入口、解析器和“转换旧 SQLite”入口及 Rust 转换模块已移除；后续产品开发不再依赖旧版软件内容。
- 已发布 SQLite 迁移中的历史兼容列保留不变，以保证已有数据库仍可打开；当前业务层不再生成或查询旧版导入哈希。
- 流水日期仍以完整 `YYYY-MM-DD HH:mm:ss` 保存；表格显示可在设置中选择完整或简短格式。
- 流水列表列顺序通过表头六点拖拽手柄调整，并保存到应用设置。

## 9. 实施检查点（2026-08-09）

### 已完成

- 已创建 React、TypeScript、Vite、Tailwind CSS、shadcn/ui 风格组件和 Tauri v2 工程。
- 已建立响应式应用外壳：桌面侧边导航、窄屏底部导航和全局快速记账入口。
- 已实现概览、流水列表、导入账单、选项管理和设置页面，以及桌面表格、手机卡片、筛选、排序、草稿、批量编辑、复制和删除流程。
- 已实现 SQLite Repository / Service 分层，页面不直接执行 SQL。
- 已使用类型化 Tauri commands 和 Rust `rusqlite` 连接替换 SQL 插件，数据库固定连接到 EXE 同级 `data/AccountBook.db`。
- 已实现三份编号 SQL 迁移、默认账本与选项、来源分类映射和应用设置。
- 已实现支付宝、微信、标准 CSV 的解析与预览，以及标准 CSV 导出；历史 Excel 入口已移除。
- 旧 SQLite 转换属于已取消的历史迁移方案，当前产品不再提供该入口或实现。
- 已实现完整备份包、SHA-256 校验、SQLite 完整性检查、暂存恢复和恢复前回滚备份。
- 已预留 `SyncService`，首版使用不联网的 `LocalOnlySyncService`。
- 已生成应用图标和 Windows Release 可执行文件。
- 已移除 `@tauri-apps/plugin-sql`、Rust SQL 插件依赖及其 ACL；当前 SQLite 读写不再依赖插件权限。
- 已将 WebView2 数据目录固定到 EXE 同级 `data/webview/`，不使用 Roaming AppData 或 LocalAppData。
- 已在设置页加入“退出时清理 WebView2 浏览数据”开关；关闭窗口时使用 Tauri 原生 API 清理缓存、Cookie 和站点数据，不影响账本数据库或备份。
- 首次启动会创建 `data/`、`backups/` 和数据库；临时目录或不可写目录会返回包含实际路径的明确错误。
- 完整备份默认保存到 EXE 同级 `backups/`，仍允许用户选择其他位置；旧库迁移备份和恢复回滚也统一存放于此。
- 已使用本地真实支付宝和微信账单修正并验证导入规则；`records/` 已作为隐私目录忽略。

### 自动验证结果

- 前端测试：5 个测试文件、14 项测试通过，包含批量编辑事务、混合换行、退款、中性交易过滤及真实账单解析。
- 真实微信账单：43 条来源记录中 41 条候选、2 条“转入零钱通”过滤；14 条收入和 27 条支出均成功解析。
- 真实支付宝账单：128 条来源记录中 82 条候选、46 条过滤；候选包含 2 条收入、77 条支出和 3 条退款，关闭交易不导入。
- Rust 测试：7 项通过，包括便携数据库首次创建、退出清理偏好读取、临时目录拒绝、不可写目录错误、备份恢复、真实旧库转换和统计对照。
- 真实旧库转换：3,483 条全部插入，0 条跳过；逐笔 ID、日期、金额、收支类型和分类一致。
- 旧数据库在转换前后字节一致，转换前备份与源文件一致。
- 历史分类“礼物”和“转账红包”转换后以停用分类保留。
- 旧库全部月份的个人收支、交易数、分类净支出、收入/支出标签、过手资金和报销余额与新版统计 SQL 一致。
- 完整备份恢复往返通过，恢复前自动生成回滚数据库。
- TypeScript 生产构建、`cargo check`、`cargo clippy -D warnings` 和 Tauri Release 构建通过。
- `npm audit --audit-level=moderate`：0 个已知漏洞。
- 桌面和 390px 窄屏完成概览、流水、快速记账、导入和设置页面视觉验收；未发现页面级横向溢出或控制台错误。
- 根目录原生程序能够启动，数据库位于 `data/AccountBook.db`，WebView2 数据位于 `data/webview/`。
- 启动前后均不存在 `%APPDATA%\com.codex.accountbook` 和 `%LOCALAPPDATA%\com.codex.accountbook`。
- 已启用退出清理偏好并通过正常窗口关闭验证：应用成功退出，数据库仍存在且流水数量不变；清理仅针对 WebView2 浏览数据。

### 便携版构建

- `npm.cmd run portable` 执行 Tauri Release 构建，构建脚本只把最终程序复制为项目根目录 `AccountBook.exe`。
- 若根目录 `AccountBook.exe` 正在运行，构建脚本仅强制关闭可执行路径与目标完全匹配的实例；2026-08-09 已使用 PID 3552 实际验证。
- 构建脚本不创建或清空 `data/`、`backups/`、README、资源目录或 ZIP。
- `tauri.conf.json` 已设置 `bundle.active = false`，不生成 MSI 或 NSIS 安装程序。
- 根目录 EXE 按用户要求不再忽略；`records/`、实际数据库、WebView 缓存、真实备份、Rust 构建目录和本地工具安装文件保持忽略。
- 仓库使用 `data/.gitkeep` 和 `backups/.gitkeep` 保留空目录；上传 GitHub 时由用户统一包含根目录 EXE、必要资源、空目录和 `README.md`。
- 便携版依赖目标 Windows 系统已安装 Microsoft Edge WebView2 Runtime，不需要 WiX Toolset。
- 根目录便携产物已重新构建并启动；数据库、WebView 和备份目录均位于应用目录，未再出现 SQL ACL 错误，也未生成便携 ZIP。

## 10. 后续工作

- 获得真实历史 Excel 样本后补充端到端导入对照测试。
- 补充真实账单重复导入、来源分类映射和提交事务结果测试。
- 首版功能稳定后，再评估 Supabase 登录、云备份、多设备同步和多人账本。
