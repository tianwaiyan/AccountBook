# 更新记录


## 2026-08-10

- “取消筛选”现在同时清除账户、收支、分类、标签、状态、金额范围以及时间/金额排序；只要存在任一筛选或排序条件，按钮就会启用。
- 移除流水页面首次打开时的时间降序默认；前端、SQLite Repository 和浏览器演示数据在未指定排序时均保持无时间排序状态。
- 桌面流水表格行高由 44px 收紧为 36px，减少列表空间占用；编辑输入控件高度和行定位同步调整。
- 同步虚拟化列表的行高估算与滚动定位为 36px，避免浏览态出现旧行高造成的空隙。
- 浏览态文本单元格统一使用单行省略号，并通过悬停 `title` 显示完整内容；窄屏卡片标题改为最多两行截断并保留完整标题提示。
- 新增回归测试，覆盖无默认排序、取消筛选清除排序、紧凑行高、桌面文本截断和窄屏两行截断。
- 验证通过：前端 8 个测试文件共 28 项测试、`npm.cmd run build`、`npm.cmd run portable`；根目录 `AccountBook.exe` 已重新生成并实际启动验证，`data/AccountBook.db` 未改变。

## 2026-08-10

- 流水列表日期显示切换统一为完整 `YYYY-MM-DD HH:mm:ss` 与简短 `YYYY-MM-DD` 两种格式；仅改变界面显示，数据库仍保存完整本地日期时间。
- 移除流水表头原有六点拖动手柄，改为长按表头约 450ms 后拖动调整列顺序；列宽调整仍使用表头右侧独立区域。
- 时间和金额表头菜单增加“取消排序”，并支持无排序状态；修改草稿时“取消筛选”按钮继续保持可用。
- 所有金额输入改用文本小数输入，移除上下箭头，允许编辑过程中保留中间输入内容，保存草稿时再按收支类型四舍五入到两位小数。
- 修正 Radix 下拉菜单回归测试的指针事件模拟，并补充排序菜单重新渲染后的稳定性检查。
- 验证通过：前端 8 个测试文件共 27 项测试、`npm.cmd run build`、`npm.cmd run portable`；根目录 `AccountBook.exe` 已重新生成并实际启动验证，固定 `data/AccountBook.db` 未被改变。

## 2026-08-10

- 修复修改流水时输入框在每次按键后失去焦点的问题：列定义不再依赖逐字变化的草稿行数据，表格行改用流水 UUID 作为稳定 ID，输入期间继续保留内存草稿。
- 新增流水编辑回归测试，覆盖桌面备注、手机卡片时间和金额输入的连续修改与焦点保持。
- 本轮验证通过：8 个前端测试文件共 24 项测试，以及 TypeScript/Vite 生产构建。

- 概览饼图新增悬停突出显示：当前扇区会外扩并加粗边界，移开后恢复原状。
- Tauri 主窗口默认最大化；使用根目录 `AccountBook.exe` 实际启动检查，窗口标题为 AccountBook，Win32 窗口状态为最大化。
- 导入账单移除“旧版 Excel”标签页、解析器和相关类型；恢复区域移除“转换旧 SQLite”按钮、BackupService 方法、Tauri command 注册及 Rust 旧库转换模块。
- 外部导入去重统一只使用版本化 `import_fingerprint`；已发布 SQLite 迁移中的历史兼容列保留不变，但当前业务层不再生成或查询旧版导入哈希。
- 流水列表增加“流水日期显示”设置：默认完整显示 `YYYY-MM-DD HH:mm:ss`，可切换为 `MM-DD HH:mm`；数据库和编辑输入仍保持完整本地日期时间。
- 流水表头六点拖拽手柄补充可访问名称和用途提示，列顺序继续保存到应用设置，支持用户自主调整。
- README.txt 和 migration-notes.md 已同步移除当前产品的旧 SQLite 迁移说明，并标明历史入口已取消。
- 本轮验证通过：7 个前端测试文件共 21 项测试、TypeScript/Vite 生产构建、`cargo fmt --check`、5 项 Rust 单元测试、`cargo check`、严格 Clippy 和 `npm.cmd run portable`；根目录 `AccountBook.exe` 已更新。

- 月度收支趋势重构为同花顺式布局：顶部固定图例，左侧纵轴固定，中间曲线区域独立横向滚动，底部月份轴和 0 轴固定并对齐；保留收入、支出两条直线，不引入蜡烛 K 线数据。
- 趋势图按当前可视月份窗口动态计算纵轴最大值，纵轴域固定为 0 到当前窗口最大值；月份缩放最低为 12 个月，缩放时尽量保持当前左侧月份位置。
- 新增趋势窗口计算、可视范围、纵轴刻度和金额转换测试；前端测试由 14 项增加到 19 项。
- 本轮验证通过：19 项前端测试、TypeScript/Vite 生产构建、便携 Release 构建、桌面趋势图检查和 390px 窄屏检查；窄屏无页面级横向溢出，纵轴显示 0 与顶部最大刻度；根目录 `AccountBook.exe` 已更新。
- 流水桌面表头筛选改为字段名称右侧的下拉箭头，移除额外筛选行；账户、收支、分类、标签和状态支持菜单内连续多选，选择后菜单保持打开，点击外部关闭。
- 时间表头新增升序/降序菜单；金额表头把升降序与金额范围合并到同一菜单。“修改流水”右侧新增“取消筛选”按钮。
- 筛选查询期间保留当前表格，避免表头卸载造成菜单意外关闭；桌面表头高度同步收紧。
- 月度收支趋势改为固定左侧纵坐标与独立横向滚动绘图区，折线改为直线连接；新增放大/缩小按钮，显示月份数最低为 12，纵轴范围固定从 0 到当前图表最大值。
- 概览饼图参考用户提供的示例，将分类名称和占比放入扇面内部，缩小内圈并将扇面间距降至 0.4 度。
- 本轮验证通过：前端 5 个测试文件共 14 项、TypeScript/Vite 生产构建和便携 Release 构建；实际浏览器验证筛选菜单可连续选择并在点击外部后关闭，桌面及 390px 窄屏无页面级横向溢出；根目录 `AccountBook.exe` 已更新。
- Release EXE 增加 Windows GUI 子系统标记，实际启动时仅出现 AccountBook 主窗口和 WebView2 子进程，不再打开黑色控制台窗口。
- 侧边栏名称更新为“流水列表”“导入账单”，新增“选项管理”页面；账户、支出/收入分类和支出/收入标签编辑器从设置页迁出，编辑 UI 保持原有布局和交互。
- 设置页现在只保留“本地数据”和“同步”；右上角“记一笔”在设置和选项管理页面隐藏，移动端底部记账入口仍保留。
- “退出时清理 WebView2 浏览数据”默认勾选；未保存过偏好的数据库也按开启处理，用户主动关闭后仍保留关闭选择。
- 流水账户、收支、金额、分类、标签和状态筛选合并到桌面表格对应表头；手机卡片视图保留紧凑筛选区。
- 列宽拖动改为独立调整手柄，列顺序拖动不再占用调整手柄；列宽保存采用延迟写入，避免鼠标移动期间持续写 SQLite。
- 概览月度趋势改为 12 个月可视窗口，历史月份通过下方横向滚动条浏览；三个饼图在桌面同排，移除图例并直接标注扇区名称。
- 本次验证通过：前端 5 个测试文件共 14 项、TypeScript/Vite 生产构建、Rust 格式检查、7 项 Rust 测试、`cargo check`、严格 Clippy 和便携 Release 构建。

## 2026-08-09

- 新增设置页“本地数据”选项，可启用退出时清理 WebView2 缓存、Cookie 和站点数据；关闭窗口时由 Rust 调用 Tauri 原生清理 API，账本数据库和备份不受影响。
- 将 SQLite 从 Tauri SQL 插件迁移到类型化 Rust commands 与持久 `rusqlite` 连接，数据库固定为根目录 `data/AccountBook.db`，并移除 SQL 插件依赖和 ACL。
- 首次启动现自动创建 `data/`、`backups/` 和数据库；新增临时目录拒绝及不可写目录清晰错误测试。
- WebView2 数据固定为 `data/webview/`；清理应用专属 AppData/LocalAppData 后，使用最终 EXE 验证启动前后均未重新生成残留目录。
- 完整备份、旧库迁移备份和恢复回滚默认统一到根目录 `backups/`，保存完整备份时仍可选择其他位置。
- 便携构建脚本改为只生成根目录 `AccountBook.exe`，不创建 ZIP、目录或说明文件；实际验证脚本能够强制关闭路径完全匹配的运行实例 PID 3552 后完成构建。
- 修复 `AGENTS.md` 的中文编码损坏，补全便携存储、构建输出、GitHub 文件和精确路径关闭进程规则；同步更新 `README.md`、`README.txt` 与迁移记录。
- 本轮验证通过：前端 5 个测试文件共 14 项、TypeScript/Vite 生产构建、Rust 格式检查、7 项 Rust 测试、`cargo check`、严格 Clippy 和 Tauri Release 构建。
- 最终 `AccountBook.exe` 启动验证通过：`data/AccountBook.db`、`data/webview/` 和 `backups/` 均存在，AppData/LocalAppData 应用目录不存在，且没有生成 `AccountBook-Portable.zip`。
- 已启用退出清理偏好并用正常窗口关闭验证：应用成功退出，SQLite 数据库与流水数量保持不变，WebView2 数据目录继续位于 `data/webview/`。
- 以下 SQL ACL 修复记录属于较早阶段；当前实现已由本次 Rust SQLite 适配器替代。
- 修复 Tauri SQL 插件 ACL，新增 `sql:allow-execute`，恢复 SQLite 写入、迁移和各项业务功能。
- 修复支付宝 CSV 混合换行解析，显式兼容 GB18030 和逗号分隔。
- 支付宝明确退款记录现按退款导入；其他“不计收支”和“交易关闭”记录进入过滤清单。
- 使用 `records/` 中真实微信和支付宝账单完成自动化导入测试：微信 43 条中 41 条候选、2 条过滤；支付宝 128 条中 82 条候选、46 条过滤。
- 便携构建改为在项目根目录生成 `AccountBook.exe`。
- 将真实账单目录和根目录 EXE 加入 `.gitignore`。
- 精简 `AGENTS.md`，补充便携发布、ACL、真实数据隐私和每次更新维护本记录的规则。
- 新增 `旧版功能差异.md`，冻结尚未实现、已替代和明确不采用的功能清单。
- 前端自动测试共 5 个测试文件、14 项测试通过。
- 当时的 TypeScript 生产构建、Rust 格式检查、3 项 Rust 测试、`cargo check` 和 Clippy 均通过；当前验证数量见本节上方的新记录。
- 已重新生成并启动根目录 `AccountBook.exe`；概览页正常显示 SQLite 数据，未再出现 SQL ACL 错误。旧 `release/` 产物目录已移除。

## 2026-08-10

- 修复流水编辑焦点问题：编辑模式下表格继续使用稳定的流水 UUID 作为行标识，草稿值通过内存引用读取，不再用每次按键变化的草稿对象替换表格数据。
- 桌面流水表格在编辑模式下停止行虚拟化，避免正在输入的行被虚拟列表回收；浏览模式继续使用虚拟化。
- 文本、金额和下拉编辑控件改为稳定的记忆化组件；未保存状态回调不再在每次草稿变化时先触发清理状态。
- 前端测试 8 个测试文件、24 项测试通过；TypeScript/Vite 生产构建通过。
- 通过 `npm.cmd run portable` 重新生成根目录 `D:\Codex\AccountBook\AccountBook.exe`，构建脚本完成了根目录进程检查和覆盖。
- 启动根目录 EXE 验证通过：应用进程可正常启动，`data/AccountBook.db`、`data/webview/` 和 `backups/` 均存在；验证进程随后已关闭。
- 编辑模式下复制流水改为优先复制当前内存草稿；修正后重新通过前端测试、生产构建和便携构建。
- 最终根目录 `AccountBook.exe` 于 14:41:38 启动验证通过，匹配路径的验证进程已关闭。

## 2026-08-10

- 优化流水界面配色：应用内筛选菜单和 Radix Select 的选中、高亮状态改为浅蓝灰背景、主蓝文字和蓝色勾选图标，普通悬停仍保持中性浅色。
- 流水编辑输入框和原生下拉框的聚焦边框改为主蓝色，并增加低强度蓝色外环；未聚焦状态保持透明边框和白色背景。
- 流水表头增加极浅蓝灰底色，已选流水增加浅蓝层次；收入、支出和待处理状态颜色保持不变，未修改全局 accent。
- 前端测试 8 个测试文件、24 项测试通过，TypeScript/Vite 生产构建通过。
- 通过 `npm.cmd run portable` 重新生成根目录 `D:\Codex\AccountBook\AccountBook.exe`；启动验证通过，固定数据目录存在，验证进程已关闭。

## 2026-08-10

- 草稿流水页的账户、收支、分类、标签和状态行内下拉改为应用内 Radix Select，保留空值、禁用状态和原有草稿更新逻辑。
- 草稿下拉选中项、高亮项和勾选图标统一使用浅蓝灰与主蓝色；普通流水浏览页的表格背景和选中行样式不受编辑态视觉增强影响。
- 草稿输入框与下拉触发器改为与全局搜索框一致的 `border-input` 和 `focus-visible:ring-2 focus-visible:ring-ring` 焦点样式。
- 测试环境补充 Radix Select 所需的 Pointer Capture 和 `scrollIntoView` jsdom polyfill；新增草稿下拉选中样式回归测试。
- 前端测试 8 个测试文件、25 项测试通过，TypeScript/Vite 生产构建和便携构建通过。
- 根目录 `AccountBook.exe` 于 16:05:11 启动验证通过，`data/AccountBook.db`、`data/webview/` 和 `backups/` 均存在，验证进程已关闭。

## 2026-08-10

- 仅优化修改草稿界面：单元格内编辑控件取消圆角矩形和可见边框，改为白底、紧凑内边距，普通流水浏览页保持原样。
- 草稿页表头改为不透明 `bg-muted`，表格编辑背景改为纯白，滚动时表头不会透出内容；编辑单元格左右间距收紧。
- 草稿页 Radix 下拉移除选中项勾选符号，并在无勾选模式下收紧菜单文字左侧留白；下拉箭头保留。
- 草稿金额输入改为文本小数输入，取消上下箭头；输入过程中保留 `20.`、`20.125` 等中间文本，保存修改时才按收支类型四舍五入并保留两位小数。
- 新增金额草稿文本与整数分的分离处理，复制、取消、切换收支和批量草稿均保持原有行为；无效金额会在保存时明确报错。
- 更新流水编辑回归测试，验证金额连续输入、焦点保持、保存时 `20.125` 转为 `-2013` 分，以及草稿下拉不显示勾选图标。
- 本轮验证通过：前端 8 个测试文件、25 项测试，TypeScript/Vite 生产构建和 `npm.cmd run portable`；根目录 `D:\Codex\AccountBook\AccountBook.exe` 于 16:40:38 更新并启动验证通过。
- 桌面草稿页与 390px 窄屏预览通过：无页面级横向溢出，编辑表头保持不透明，草稿下拉和金额输入显示正常；验证进程及本地预览服务已关闭。

## 2026-08-10

- 修复流水表头长按拖动列顺序：所有非选择列表头均可从标题或嵌套筛选/排序按钮启动 450ms 长按拖动；长按计时期间自然移动不会取消拖动。
- 新增指针捕获、`pointercancel`、`lostpointercapture` 和基于 `elementFromPoint` 的目标列识别；拖动开始会关闭表头菜单并抑制本次指针序列的普通点击。
- 选择列和列宽调整区域保持不可拖动；列顺序继续按稳定列 ID 更新并持久化到 `transaction_column_order` 设置。
- 表头筛选/排序菜单改为受控打开状态，短按和键盘操作保持原有菜单行为。
- 新增长按重排、嵌套菜单触发、自然移动、短按菜单、列宽手柄、`pointercancel` 和 `lostpointercapture` 回归测试。
- 验证通过：前端 8 个测试文件、32 项测试，`npm.cmd run build` 和 `npm.cmd run portable`；根目录 `D:\Codex\AccountBook\AccountBook.exe` 已更新并启动验证，窗口标题为 AccountBook，主进程和 WebView2 子进程正常，验证进程已关闭。
- 补充长按阈值前的悬停目标跟踪；最终构建完成并再次启动验证通过，固定数据目录未被改变。

## 2026-08-11

- 精简并更新 `AGENTS.md`：按当前项目目录、Tauri 便携存储、SQLite 数据规则、导入备份、UI、构建发布和验证流程重新整理长期约定。
- 删除已经过时或不适合放入开发规范的细节，保留当前实际支持的支付宝/微信及标准 CSV 导入范围。
- 新增项目修改后的文档维护和 Git 流程要求：更新本记录、检查 `git status`、避免提交用户数据，并为本次修改创建本地 commit。

## 2026-08-11

- 重写 `README.md` 为面向使用者的中文说明，补充快速启动、运行要求、数据位置、备份、隐私、主要功能和常见开发命令。
- 删除项目根目录 `README.txt`，并同步将发布说明中的文档入口改为 `README.md`。
- 更新 `migration-notes.md` 中的 GitHub 发布文件说明，移除对已删除文本说明文件的引用。

## 2026-08-11

- 再次调整 `README.md`，改为面向使用者的产品说明，重点介绍本地优先、免安装、快速记账、流水整理、账单导入、概览统计和备份恢复等特色与优势。
- 移除 README 中的开发者命令、内部技术说明和开发流程内容，保留普通用户需要的启动、运行要求、数据位置和隐私提示。

## 2026-08-11

- 为桌面流水表格的列顺序拖动增加移动预览：长按激活后源列表头保留原占位并跟随指针平移、半透明提升显示，目标列左侧显示带脉冲动画的插入指示线。
- 拖动状态增加可测试的数据属性；取消、指针丢失和释放时会清理位移、阴影和目标提示，列宽手柄、短按菜单和原有列顺序持久化逻辑保持不变。
- 新增列拖动视觉状态回归断言，验证 450ms 激活、指针位移、目标指示线和取消清理行为。
- 验证通过：前端 8 个测试文件、30 项测试通过（2 项跳过），TypeScript/Vite 生产构建通过。

## 2026-08-11

- 将项目收尾规则明确为：每次修改完成并通过基础验证后，必须执行 `npm.cmd run portable`，由 `scripts/build-portable.ps1` 重新生成根目录 `AccountBook.exe`。
- 已执行便携构建并成功生成 `D:\Codex\AccountBook\AccountBook.exe`；构建过程包含 TypeScript/Vite 前端构建和 Tauri Release 编译。

## 2026-08-11

- 新增“批量记账”入口，支持选择多个固定账目预设并按目标月份一次生成全部实际日期；重复执行同一预设同一月份会跳过。
- 选项管理新增“月度预设”标签页，支持账户、收支、金额、分类、标签、状态、备注、交易对方、支付方式、启用状态和默认勾选设置。
- 新增固定账目日期规则编辑器，支持每月固定日期、月底、每月第一个/第二个/最后一个星期几、每周星期几和每年指定月日；预览未来三次日期，并处理短月份和闰年日期。
- 日期规则使用结构化 JSON 保存，服务层使用 `rrule.js` 计算日期；界面不显示 Cron 或 RRULE 字符串。
- 新增 SQLite `0004` 迁移、预设运行记录和生成流水关联；生成流水使用 `source = 'preset'`，仍可按普通流水编辑。
- 新增规则服务、预设服务、SQLite/演示仓库和前端回归测试；已完成前端全量测试、TypeScript/Vite 构建、Rust 格式检查、测试、`cargo check` 和严格 Clippy 验证。
